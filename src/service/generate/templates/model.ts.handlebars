{{#each foreignKeys}}
import { {{this}} } from './{{this}}';
{{/each}}

export interface {{name}} {
{{#each properties}}
{{#if description}}
    /**
     * {{description}}
     */
{{/if}}
    {{name}}{{#unless isRequired}}?{{/unless}}: {{type}};
{{/each}}
}